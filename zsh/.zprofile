# Servers
alias l='ls -lh'
alias ll='ls -la'
alias ls="ls -G"
alias upgrade='sudo apt-get update && sudo apt-get upgrade'

# Projects
alias code='cd code'

# Laravel
alias art='php artisan'
